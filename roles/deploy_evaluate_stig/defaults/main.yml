---
# Default values for deploy_evaluate_stig role

# UNC path to the source Evaluate-STIG folder on the network share
src_share: "\\\\192.168.40.42\\raw-images\\Evaluate-STIG"

# Local destination root for copying the folder
dest_root: "C:\\ProgramData"

# SMB credentials (set via AWX Credential or extra_vars)
smb_user: ""
smb_pass: ""
